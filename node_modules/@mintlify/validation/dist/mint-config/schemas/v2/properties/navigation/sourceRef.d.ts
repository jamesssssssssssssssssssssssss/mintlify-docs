import { z } from 'zod';
import type { DocsConfig } from '../../index.js';
export declare const sourceRefSchema: z.ZodObject<{
    sourceRef: z.ZodString;
}, "strip", z.ZodTypeAny, {
    sourceRef: string;
}, {
    sourceRef: string;
}>;
export type SourceRefConfig = z.infer<typeof sourceRefSchema>;
type WithSourceRefs<T> = T extends string ? T : T extends readonly (infer U)[] ? (WithSourceRefs<U> | SourceRefConfig)[] : T extends object ? {
    [K in keyof T]: WithSourceRefs<T[K]>;
} : T;
export type SourceRefNavigationConfig = WithSourceRefs<DocsConfig['navigation']>;
export type SourceRefDocsConfig = Omit<DocsConfig, 'navigation'> & {
    navigation: SourceRefNavigationConfig;
};
export {};
