import { z } from 'zod';
export const sourceRefSchema = z
    .object({
    sourceRef: z.string().nonempty().describe('The mount path of the git source to inline'),
})
    .describe('Reference to a git source navigation section');
