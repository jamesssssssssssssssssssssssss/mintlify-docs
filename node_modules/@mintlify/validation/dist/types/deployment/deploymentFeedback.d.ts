export type DeploymentFeedbackType = {
    thumbs?: boolean;
    edits?: boolean;
    issues?: boolean;
    agentFeedback?: boolean;
    /** Forward page-feedback signals to the configured analytics integration (e.g. Adobe). */
    analyticsForwarding?: boolean;
    contextualFeedback?: {
        textInputOnly?: boolean;
        allowEmail?: boolean;
        emailDisclaimer?: string;
    };
};
